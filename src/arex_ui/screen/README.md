# screen

`screen` 放屏幕级结构和布局逻辑。

主要职责：

- 创建根屏、safe zone、左侧锚点、右侧 tileview、wall 和 dots。
- 计算左侧固定网格、右侧卡片和 5F 自定义网格布局。
- 维护卡片注册表和 card order 的显示位置映射。
- 提供状态机、卡片、告警和组件刷新所需的屏幕门面 API。

修改屏幕区域、布局尺寸、卡片顺序映射或页面级导航时，优先看这里。
